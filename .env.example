# URL do backend NestJS (auth + leads)
NUXT_PUBLIC_API_BASE=http://localhost:3333

# Origem pública do site (usada em sitemap.xml e robots.txt).
# Produção: https://seu-dominio.com.br · Dev: deixe vazio (deriva do host).
NUXT_PUBLIC_SITE_URL=
